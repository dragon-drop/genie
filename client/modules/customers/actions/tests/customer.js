const {describe, it} = global;
import {expect} from 'chai';
import {spy, stub} from 'sinon';
import actions from '../customer';

describe('customers.actions.customer', () => {
  it('should do something');
});
