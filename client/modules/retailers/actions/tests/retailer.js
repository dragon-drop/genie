const {describe, it} = global;
import {expect} from 'chai';
import {spy, stub} from 'sinon';
import actions from '../retailer';

describe('retailers.actions.retailer', () => {
  it('should do something');
});
